export enum BreakPointEnum {
  XS = "xs",
  SM = "sm",
  MD = "md",
  LG = "lg",
  XL = "xl"
}

export class Constants {
  static readonly BASE_URL = "https://meikou-api.itheima.net/" // 基地址
  static readonly SUCCESS_CODE = 1 // 请求成功码
  static readonly TIME_OUT = 10000 // 请求超时时间
}